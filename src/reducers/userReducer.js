const initialState = {
    // Başlangıç state'i
  };
  
  const userReducer = (state = initialState, action) => {
    switch (action.type) {
      // action tiplerine göre state güncellemeleri
      default:
        return state;
    }
  };
  
  export default userReducer;
  